{{--
  The ambient light, carried over from Trailhead Labs so the two sites
  read as one hand. Each blob is two elements so the drift and any
  reveal never fight over a single transform. Dark mode only.
--}}
<div class="blob-field" aria-hidden="true">
    <div class="blob blob-1"><i></i></div>
    <div class="blob blob-2"><i></i></div>
    <div class="blob blob-3"><i></i></div>
</div>
